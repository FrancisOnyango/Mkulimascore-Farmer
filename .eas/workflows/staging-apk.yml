name: Staging APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  android_staging_apk:
    name: Android staging APK
    type: build
    params:
      platform: android
      profile: staging
